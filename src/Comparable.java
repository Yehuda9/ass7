/**
 * The interface Comparable.
 *
 * @param <T> the type parameter
 */
public interface Comparable<T> {
    /**
     * Compare to int.
     *
     * @param hyponym hyponym
     * @return int
     */
    int compareTo(T hyponym);
}
